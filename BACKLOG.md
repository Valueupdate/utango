# utango バックログ（課題・改善管理リスト）

最終更新: 2026-08-31

---

## 凡例

- 🔴 高: ユーザーが使えない・重大な不具合
- 🟡 中: 体験を損なう・運用上の問題
- 🟢 低: あると嬉しい改善

| 状態 | 意味 |
|------|------|
| ⬜ 未着手 | まだ作業していない |
| 🔧 作業中 | 実装・修正中 |
| ✅ 完了 | 実装・デプロイ済み |

---

## UX・フロントエンド

| # | 優先度 | 状態 | 課題 | 詳細 | 対応方針 |
|---|--------|------|------|------|----------|
| U-06 | 🟡 中 | ⬜ | ジャンル選択UIがない | `MUSIC_STYLE_PROMPT` がバックエンドにハードコードされており、ユーザーがジャンル（CMジングル / J-Pop / ラップ / バラード等）を選ぶ手段がない | 初期画面にジャンル選択UIを追加し、`/sing` に `genre` を送信。`music_service` でプロンプトを動的に切り替える（B-03 とセット） |
| U-08 | 🟡 中 | ⬜ | 楽曲生成中に進捗の手がかりがない | `state === "singing"` はメッセージのみ表示。実際は Lyria 待ちで数十秒〜数分かかり、固まったように見える | 経過秒数の表示、または擬似プログレスバー（時間ベース）を追加する。SSE の keepalive を活かして「生成中（○秒経過）」を出す |
| U-09 | 🟢 低 | ⬜ | ProgressView.tsx がデッドコード | 3段階API化に伴い未使用になったが残存。`page.tsx` の `LogEntry` 型も未使用 | U-08 の実装で再利用するか、しないなら削除して `LogEntry` も除去する |
| U-10 | 🟢 低 | ⬜ | 抽出エラー時に画像が失われる | `handleExtract` の失敗で `state = "error"` になり、`handleReset` しか出口がないため画像を選び直す必要がある | エラー画面に「同じ画像で再試行」ボタンを追加する（U-05 の残り） |
| U-11 | 🟢 低 | ⬜ | 単語数の上限がユーザーに見えない | `MAX_WORDS_PER_SONG` で切り捨てられるが、UI に説明がなく「読み取れなかった」と誤解される | 抽出結果画面に「1曲に入れられるのは○語までです」と明示する |

---

## バックエンド・インフラ

| # | 優先度 | 状態 | 課題 | 詳細 | 対応方針 |
|---|--------|------|------|------|----------|
| B-04 | 🔴 高 | ⬜ | `response.parts` が None でクラッシュ | `music_service.generate_music` が `for part in response.parts:` を直接回している。google-genai は安全性ブロック時やテキスト応答時に `parts` が None を返し、`TypeError` になる。原因が「楽曲生成エラー」に埋もれる | `response.candidates[0].content.parts` を安全に辿り、`finish_reason` / `prompt_feedback` をログとエラーメッセージに含める |
| B-05 | 🔴 高 | ⬜ | SSE 切断で進捗を復元できない | `/progress` 接続時に `status` を `processing` にして生成タスクを起動する設計のため、通信が切れて再接続しても2度目はタスクが起動せず、キューに残ったイベントも取り逃す。完成していても永久に待つ | 接続直後に現在の `status/progress/message`（完了なら `done` + `download_url`）を必ず1件送る。併せて `GET /result/{job_id}` を追加してポーリングでも復帰できるようにする |
| B-06 | 🟡 中 | ⬜ | API キーがジョブに残留する | `/sing` が `job.meta["api_key"]` にキーを入れ、ジョブ有効期限（30分）までメモリ上に残る。UI の「キーはサーバーに保存されません」という説明と厳密に食い違う | `run_sing` の完了・失敗時に `job.meta` を破棄（`finally` で `job.meta = None`）。UI 文言も「処理中のみメモリ上で保持」に精緻化する |
| B-07 | 🟡 中 | ⬜ | `/extract` がジョブを作り捨てる | 画像の一時保存のためだけに `job_manager.create_job()` を呼び、以後そのジョブは使われず期限切れまで残る。ジョブIDの無駄な発行とファイル残留 | 抽出用の一時ファイルは `tempfile` で処理し、終了時に削除する。ジョブは `/sing` のみで作る |
| B-01 | 🟡 中 | ⬜ | キャンセルがフロントエンドでしか効かない | `abortRef.current.abort()` は SSE の fetch をキャンセルするだけで、バックエンドの `run_sing`（Lyria 呼び出し）は止まらない。API コストが発生し続ける | ジョブにキャンセルフラグを追加し、`POST /cancel/{job_id}` で立てる。各ステップ間でフラグを確認して中断する |
| B-03 | 🟡 中 | ⬜ | ジャンル選択のバックエンド対応 | フロントから送られたジャンル値を受け取り `MUSIC_STYLE_PROMPT` を組み立てる仕組みが未実装 | `config.py` にジャンル→プロンプトのマッピング辞書を定義。`/sing` の Form に `genre` を追加し `music_service` で参照する |
| B-08 | 🟢 低 | ⬜ | ポート既定値の不一致 | `config.PORT` の既定は 8000、`main.py` の `__main__` は 8001、systemd は 8001。起動方法によって待ち受けポートが変わる | `main.py` で `config.PORT` を参照するよう統一し、VPS は `.env` の `PORT=8001` で制御する |
| B-09 | 🟢 低 | ⬜ | 静的配信のマウントが `/` で全掛かり | `StaticFiles` を `/` にマウントしているため、今後 API を増やすときに定義順の事故が起きやすい | API を `/api` プレフィックスに寄せる（フロントの fetch 先も合わせて変更）。Nginx 側の location も整理する |

---

## ドキュメント・管理

| # | 優先度 | 状態 | 課題 | 詳細 | 対応方針 |
|---|--------|------|------|------|----------|
| D-01 | 🟡 中 | ⬜ | REQUIREMENTS.md が現状と乖離 | §3.3 が「サーバー集約方式」のままだが実装は BYOK。§4.1 の状態欄も3段階API化を反映していない | 現在の実装（BYOK・3段階API・歌詞編集）に合わせて更新する |
| D-02 | 🟡 中 | ⬜ | モデル・単語数の記述が実装と不一致 | `LYRIA_MODEL` は `lyria-3-clip-preview`（クリップ）だが README / REQUIREMENTS / SETUP は「Lyria 3 Pro」。`MAX_WORDS_PER_SONG=5` に対し `lyrics-design.md` §3 は「6〜10語・曲長1.5〜2分」 | 実装を正とするか設計を正とするか決めて統一する。30秒ジングル路線なら設計書 §3・§4 を「5語・30秒・ダジャレ型」に書き換える |
| D-03 | 🟡 中 | ⬜ | SETUP.md がサーバー集約前提のまま | 「運営者キーを .env に設定」「`ready: false` なら .env を確認」と書かれているが、実装は BYOK でブラウザ入力。`/health` も常に `ready: true` を返す | BYOK 前提の手順に書き換え、`.env` のキーは開発用フォールバックである旨を明記する |
| D-04 | 🟢 低 | ⬜ | 歌詞設計書がダジャレ路線を反映していない | `lyrics-design.md` は「反復＋和訳エコー」設計だが、実装の `LYRIS_PROMPT_TEMPLATE` はダジャレ・CMジングル路線で全面的に別物 | 設計書に「§8 ダジャレ型（現行実装）」を追記し、反復型との使い分け方針を残す |

---

## 完了済み

| # | 完了日 | 内容 |
|---|--------|------|
| ✅ U-01 | 2026-08-31 | APIキー入力を画面上部に常時表示（BYOK必須化） |
| ✅ U-02 | 2026-08-31 | キー未入力時は生成ボタンを無効化し警告文を表示 |
| ✅ U-03 | 2026-08-31 | 一気通貫パイプラインの廃止により進捗停滞が解消（U-08 に引き継ぎ） |
| ✅ U-04 | 2026-08-31 | 画面順序を「APIキー→モード→画像→読み取り」に再構成 |
| ✅ U-05 | 2026-08-31 | 歌詞生成失敗時は editing に留まり単語・歌詞を保持（残りは U-10） |
| ✅ U-07 | 2026-08-31 | 歌詞を確認・編集してから楽曲生成できるフローに分離 |
| ✅ B-02 | 2026-08-31 | API を `/extract` `/lyrics` `/sing` の3段階に分割 |
| ✅ | 2026-08-30 | MIT License 追加 |
| ✅ | 2026-08-30 | GitHub リポジトリ公開（Valueupdate/utango） |
| ✅ | 2026-08-30 | ConoHa VPS デプロイ・公開（utango.valueupdate.net） |
| ✅ | 2026-08-30 | systemd サービス登録（自動起動） |
| ✅ | 2026-08-30 | deploy.bat 作成（ワンコマンドデプロイ） |
| ✅ | 2026-08-30 | SSL 証明書取得（Let's Encrypt） |
| ✅ | 2026-08-30 | DNS 設定（Wix で A レコード追加） |
| ✅ | 2026-08-30 | ハードコードされた API キーの除去 |
