# utango バックログ（課題・改善管理リスト）

最終更新: 2026-08-30

---

## 凡例

- 🔴 高: ユーザーが使えない・重大な不具合
- 🟡 中: 体験を損なう・運用上の問題
- 🟢 低: あると嬉しい改善

| 状態 | 意味 |
|------|------|
| ⬜ 未着手 | まだ作業していない |
| 🔧 作業中 | 実装・修正中 |
| ✅ 完了 | 実装・デプロイ済み |

---

## UX・フロントエンド

| # | 優先度 | 状態 | 課題 | 詳細 | 対応方針 |
|---|--------|------|------|------|----------|
| U-01 | 🔴 高 | ⬜ | APIキーが隠れているのにサーバーキーが空 | APIキー入力が `<details>` 内に「通常は入力不要」と隠されているが、サーバーのフォールバックキーが空のため、キー未入力で生成すると 400 エラーになる。ユーザーは原因がわからない | APIキー入力を画面上部に常時表示し、必須であることを明示する（BYOK必須化） |
| U-02 | 🔴 高 | ⬜ | APIキー未入力でも生成ボタンが押せる | `handleGenerate` にキーの必須チェックがなく、ファイル選択だけでボタンが有効になる。サーバーから 400 が返って初めて気づく | フロントエンドでキー未入力時はボタンを無効化（disabled）し、入力を促すメッセージを表示する |
| U-03 | 🔴 高 | ⬜ | プログレスバーが 0% のまま動かない | `/generate` POST 後に SSE 接続するが、`BackgroundTasks` の非同期実行によりキューにイベントが入る前に接続が始まる。最初の 30 秒間 keepalive しか返らず、プログレスバーが 0% で止まって見える | SSE 接続直後に「準備中」イベントを即座に送信する。またはジョブ作成直後に初期イベントをキューに入れておく |
| U-04 | 🟡 中 | ⬜ | モバイルでの入力順序の問題 | 画面順序が「モード選択→画像アップロード→生成ボタン→（隠れた）APIキー」のため、スマホで画像を撮影した後にキーが必要と気づき手戻りが発生する | 画面順序を「APIキー入力→モード選択→ジャンル選択→画像アップロード→生成ボタン」に再構成する |
| U-05 | 🟡 中 | ⬜ | エラー時の「もう一度ためす」が全リセット | `handleReset` で全状態（画像・モード・キー含む）がクリアされる。APIキーの入力ミスだった場合でも画像を再選択する必要がある | エラー時は画像・モード・キーを保持したまま再実行できる「リトライ」ボタンを追加。全リセットは別途「最初からやり直す」として残す |
| U-06 | 🟡 中 | ⬜ | ジャンル選択UIがない | `MUSIC_STYLE_PROMPT` がバックエンドにハードコードされており、ユーザーがジャンル（Jazz, J-Pop, K-Pop 等）を選ぶ手段がない | フロントエンドにジャンル選択UIを追加し、選択値をバックエンドに送信。`music_service` でプロンプトを動的に切り替える |
| U-07 | 🟡 中 | ⬜ | 歌詞を編集してから楽曲生成できない | `run_generation` が「抽出→歌詞→楽曲」を一気通貫で実行するため、歌詞確認・修正の余地がない | パイプラインを「抽出→歌詞生成→ユーザー確認・編集→楽曲生成」の2段階に分離する |

---

## バックエンド・インフラ

| # | 優先度 | 状態 | 課題 | 詳細 | 対応方針 |
|---|--------|------|------|------|----------|
| B-01 | 🟡 中 | ⬜ | キャンセルがフロントエンドでしか効かない | `abortRef.current.abort()` は SSE の fetch をキャンセルするだけで、バックエンド側の `run_generation`（Gemini API 呼び出し含む）は止まらない。APIコストが発生し続ける | ジョブにキャンセルフラグを追加し、各ステップの間でフラグを確認して中断する。フロントエンドからキャンセル API（`POST /cancel/{job_id}`）を呼べるようにする |
| B-02 | 🟢 低 | ⬜ | パイプライン分離に伴う API 設計変更 | U-07（歌詞編集）を実現するには、`/generate` を「抽出+歌詞生成」と「楽曲生成」の2エンドポイントに分ける必要がある | `/generate` → 抽出+歌詞生成まで。`/generate-music` → 編集済み歌詞を受け取って楽曲生成。SSE も各段階に対応させる |
| B-03 | 🟢 低 | ⬜ | ジャンル選択のバックエンド対応 | フロントエンドから送られたジャンル値を受け取り、`MUSIC_STYLE_PROMPT` を動的に組み立てる仕組みが未実装 | `/generate` の Form パラメータに `genre` を追加。`config.py` にジャンル→プロンプトのマッピング辞書を定義し、`music_service` で参照する |

---

## ドキュメント・管理

| # | 優先度 | 状態 | 課題 | 詳細 | 対応方針 |
|---|--------|------|------|------|----------|
| D-01 | 🟡 中 | ⬜ | REQUIREMENTS.md の要件管理リストが現状と乖離 | §3.3 で「サーバー集約方式」と記載されているが、実装は BYOK 方式に変更済み。§4.1 の状態欄も更新が必要 | REQUIREMENTS.md を現在の実装状態と方針に合わせて更新する |

---

## 完了済み

| # | 完了日 | 内容 |
|---|--------|------|
| ✅ | 2026-08-30 | MIT License 追加 |
| ✅ | 2026-08-30 | GitHub リポジトリ公開（Valueupdate/utango） |
| ✅ | 2026-08-30 | ConoHa VPS デプロイ・公開（utango.valueupdate.net） |
| ✅ | 2026-08-30 | systemd サービス登録（自動起動） |
| ✅ | 2026-08-30 | deploy.bat 作成（ワンコマンドデプロイ） |
| ✅ | 2026-08-30 | SSL 証明書取得（Let's Encrypt） |
| ✅ | 2026-08-30 | DNS 設定（Wix で A レコード追加） |
| ✅ | 2026-08-30 | ハードコードされた API キーの除去 |
